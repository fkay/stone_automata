"""
Perform A* algorithm to find solution
uses Euclidean distance for the heuristics
"""


def solve_Astar(init_map: list[list[int]]) -> list[chr]:
    """
    Solve the map, return list with movements:
    U - up
    D - down
    L - left
    R - right

    Args:
        init_map (list[list[int]]): initial map indicating start and end

    Returns:
        list[chr]: list with the movevements to solve
    """
    pass
